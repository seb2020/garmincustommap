#!/bin/bash

# Run the generate_map.sh script with the provided arguments
# Example usage for Switzerland: ./garmin/generate_map.sh "europe" "switzerland" "CH" "" 51000

./garmin/generate_map.sh "$continent" "$country" "$countryname_short" "$country_subzone" $mapid